# Аудит зоны «Обучение» — Traektoriya

> Read-only аудит. Дата: 2026-06-28. Аудитор: Claude (Opus 4.8).
> Контекст: геймифицированное обучение полевого персонала FMCG, курс на минимализм.
> Светлая тема «Пепел-холст». Карта мира и кат-сцены уроков НАМЕРЕННО тёмные (immersive) — не баг.

Легенда статусов: ✅ работает · 🟡 работает с замечаниями · 🔴 сломано/заглушка.
Приоритет: 🔴P0 (критично) · 🟡P1 (важно) · 🟢P2 (полировка).

---

## 1. Десктоп-карта обучения (TacticalLearningPage)

**Статус: 🟡**
`src/pages/TacticalLearningPage.tsx`

### Функционал
- ✅ Реальные данные из learning API через `loadLearningMapData` (`mapLearningToNodes.ts`). Fallback на mock НЕ делается — при ошибке пустая карта (строки 91-97). Это правильно.
- ✅ 1 клик по узлу = открыть курс; заблокированный узел открывает инфо-панель (строки 104-112).
- ✅ Селектор «Смотрю как…» для admin/superadmin/КД/тренера работает корректно (строки 60-63, 397-447).
- ✅ Пустое состояние «КУРСЫ НЕ НАЙДЕНЫ» с подсказкой про переключатель ролей (строки 247-257).
- 🟡 Загрузка карты = ~11 параллельных запросов (1 map + N sections) при каждой смене роли/языка (`mapLearningToNodes.ts:144-163`). Для admin при переключении ролей это заметная задержка; кэширования нет.

### Дизайн/UX
- 🟡 Две кнопки в шапке («Кейсотека», «История 2025») собраны на инлайн-стилях с дублированием `onMouseEnter/Leave` (строки 160-219). ~120 строк инлайн-CSS вместо переиспользуемого компонента — против минимализма и Кодекса. Лучше вынести в `<Button>`.
- 🟡 Шапка перегружена: заголовок + tag + селектор роли + 2 кнопки + 3 метрики. Для роли минимализма много элементов в одном ряду; на средних ширинах будет тесно.
- ✅ Светлая обвязка использует токены (`var(--brass)`, `var(--line)`, `var(--text-*)`), тёмная immersive-карта намеренна.

---

## 2. Десктоп-карта SVG (TacticalMap)

**Статус: 🔴 (по сути состояний)**
`src/components/tactical/TacticalMap.tsx`

### Функционал
- ✅ Рендер пинов, зон-полос, границ территорий, glyph по состоянию (✓/◆/▲/🔒/★).
- ✅ Принимает динамические `nodes/edges/zones`, fallback на mock из `data.ts` только если не переданы (строки 230-232) — на практике страница всегда передаёт реальные.
- 🔴 **Все узлы по факту в состоянии `available`/`new`.** Цвет пина = ТИР (территория), а не прогресс (строки 16-24, явный комментарий: «пока прогрессия по статусам не включена»). На карте `mapLearningToNodes.ts` мапит статус курса в состояние (`courseStatusToNodeState`), НО backend почти всегда отдаёт `available` (нет реальной gating-логики разблокировки на уровне курсов). Итог: пользователь не видит «где он сейчас» — пины монотонны. Это известный пункт из памяти (`project_learning_map.md`: «статусы пинов монотонны»). **🟡P1.**
- ✅ Рёбра между узлами отключены намеренно (`edges = []`, `mapLearningToNodes.ts:229`) — «как на реальной карте, только пины».

### Дизайн/UX
- ✅ Подпись курса только у выбранного пина (чтобы не зашумлять). Разумно.
- 🟡 `MapEdgeComponent` и логика рёбер (строки 97-131) — мёртвый код: edges всегда пусты. Можно удалить ~35 строк.

---

## 3. Мобильная карта (TacticalMobile)

**Статус: 🔴**
`src/components/tactical/TacticalMobile.tsx`

### Функционал
- ✅ Карта ФИКСИРОВАНА (без зума/пана) согласно решению PO 2026-06-27 (`PinchMap`, строки 277-413). `touchAction: 'pan-y'` — страница скроллится, карта стоит. Соответствует ТЗ.
- ✅ Тап по пину/тайлу открывает bottom-sheet `VillageSheet`, кнопка «Продолжить» открывает первый незаблокированный курс (строки 779-807).
- ✅ Fallback на mock-данные `data.ts`, если nodes пусты и не идёт загрузка (строки 904-908) — но т.к. узел = курс, реальные данные приходят.
- 🔴 **HeroStrip — захардкоженные фейковые цифры:** `xp={2480} streak={17} league="ЗОЛОТО II" rank={7}` (строки 963-970). Реальный только `pct` (из doneCourses). ТП видит чужой выдуманный прогресс. **🔴P0** — прямой обман в геймификации, подрывает доверие.
- 🔴 **FriendsStrip («Активность команды») — полностью mock:** Аиша/Тимур/Лейла с выдуманными действиями (строки 583-635). Не помечено как демо. **🔴P0.**
- 🔴 **DailyQuestBanner — фейковый квест** «Закрой 1 раздел сегодня +50 XP» с тикающими часами, но без связи с реальным прогрессом/наградой (строки 206-262). XP нигде не начисляется. **🟡P1.**
- 🟡 `LVL {Math.floor(xp/300)}` считается от хардкоженного xp=2480 → всегда «LVL 8» (строка 162).

### Дизайн/UX
- ✅ Мёртвые `_MobileTopBar_DEPRECATED` и `_MobileTabBar_DEPRECATED` корректно помечены `@ts-expect-error` + eslint-disable, заменены глобальными компонентами Layout. Можно удалить для чистоты (🟢P2).
- 🟡 Карта тёмная (immersive) — ок, но окружающие блоки (Hero/Quest/Friends) тоже на тёмном фоне `var(--bg-primary)` с radial-градиентами — при светлой теме нужно проверить контраст подписей `var(--text-muted)` на `oklch(0.10 ...)` фоне карты (≥4.5).

---

## 4. Плеер курса (LearningPage → CourseView)

**Статус: ✅ (с замечаниями)**
`src/pages/LearningPage.tsx` (1508 строк)

### Функционал
- ✅ Три архитектуры контента с корректным определением фазы: v3 blocks → v2 lesson_data → v1 slides (`phase` useMemo, строки 990-1017).
- ✅ Вход по `/learning/course/:id`: авто-открытие, «назад» → на Tactical-карту (строки 139-166, 243-264).
- ✅ Защита от двойного сабмита квиза (`isSubmittingQuiz || quizSubmitted`, строка 197) — предотвращает дубль CourseCompletion.
- ✅ Обработка 409 (уже завершён) — toast «info», не ошибка (строки 229-235).
- ✅ Инвалидация кэша пульса/компетенций после завершения (`emitPulseInvalidate`, строка 218).
- ✅ Для v3-блоков quiz может быть пустым → score=100, completion всё равно пишется (строки 200-204).
- 🟡 Admin видит card-view (BlockCardView) по умолчанию, юзер — fullscreen blocks (строки 990-998). Логика корректна, но `previewMode` — лишнее состояние, легко запутаться при отладке.
- 🟡 Аудио: silent-fail на `getNarration` (строка 1039) и на `audio.play()` (строки 1056, 1101) — намеренно (нет озвучки → нет плеера). Приемлемо, но при сломанном `audio_url` юзер не узнает почему молчит.

### Дизайн/UX
- 🟡 `RichSlideContent` / `parseContentBlocks` (строки 750-911) — парсер эмодзи-маркеров (✅❌🔴🟡🟢⚠️💡🎯🏆…) в цветные блоки. Это v1-контент; для immersive-слайдов ок, но логика хрупкая (regex по первому символу строки). `dangerouslySetInnerHTML` для inline-markdown (строки 761, 845-850) — XSS-риск низкий (контент из своей БД), но формально небезопасно. **🟢P2.**
- 🟡 Результат-экран и слайды используют **жёстко закодированные tailwind-цвета** (`text-gray-700`, `bg-blue-50`, `from-blue-500`) вместо токенов темы (строки 759-820, 1354-1367). При светлой теме «Пепел-холст» эти блоки не подхватят палитру → визуальный разнобой. **🟡P1** (читабельность светлой обвязки).
- ✅ Понятность прохождения: степ-точки «Материал → Тест», прогресс-бар, навигация назад/вперёд — ясно.

---

## 5. Плеер блоков v3 (BlockRunner + blocks/*)

**Статус: ✅**
`src/components/learning/blocks/BlockRunner.tsx`

### Функционал
- ✅ 13 типов блоков замаплены через `BlockContent` switch (строки 378-399): cinematic_scene, video_scene, key_point, swipe_cards, sorting, fill_blank, dialogue_choice, scenario_chain, mistake_analysis, field_task, quiz, results + fallback «Unknown block type».
- ✅ Кат-сцены/видео — fullscreen без прогресс-бара (immersive, намеренно тёмные). Корректно.
- ✅ Плавающий аудио-плеер (FAB + панель) на всех типах блоков, со скипом треков (строки 166-270).
- ✅ Score = correct/total, передаётся в completion (строки 153-156).
- ✅ `canAdvance` гейтит «Далее» для интерактивных блоков; key_point/cinematic авто-разблокируются (строки 158-163).

### Дизайн/UX
- ✅ Использует семантические токены `bg-bg-canvas / bg-bg-accent / text-fg-on-accent` (строки 303, 327, 352) — тема-aware, в отличие от v1-слайдов. Хорошо.
- 🟡 FAB аудио на не-immersive блоках в `bottom-24 right-4` (строка 181) — может перекрывать кнопку «Далее» на узких экранах. Проверить на мобиле.

---

## 6. LessonDataView (v2: Сцена/Схема/Диалог/Квиз)

**Статус: 🟡**
`src/components/learning/LessonDataView.tsx`

### Функционал
- ✅ Табы фильтруются по наличию данных (строки 32-38). Кнопка «К проверке знаний» всегда активна — табы опциональны (строки 93-104).
- ✅ Диалог-упражнение (reveal «Как правильно?» → wrong/right/lesson) и квиз с разбором работают.

### Дизайн/UX
- 🔴 **Захардкоженные русские строки игнорируют язык:** «Что на кону:» (строка 173), «Ситуация» (307), «Как правильно?» (317), «Следующая ситуация» (359), «Следующий вопрос» (501), «correct» по-английски (406), «правильно» (519). Параметр `lang` приходит, но в Scene/Dialogue/Quiz не используется. UZ-пользователь видит русский/английский. **🟡P1** (нарушает bilingual-требование).
- 🟡 Жёсткие tailwind-цвета (`from-gray-900`, `bg-red-50`, `text-amber-800`) вместо токенов — те же риски при светлой теме (строки 118-177). Часть (Scene) immersive-тёмная намеренно, но инфографика/квиз — обвязка, должна быть тема-aware.
- 🟡 Эмодзи как иконки (📍🔥⚡💡🎬📊💬✅) вместо lucide — против Кодекса «NO emoji в UI», но соответствует игровому тону. Развилка для PO.

---

## 7. VillageView (посёлок — SVG домики)

**Статус: 🟡**
`src/components/learning/VillageView.tsx`

### Функционал
- ✅ Раскладка домиков 1-7 + fallback 8+, дорожка между ними, статусы (completed/available/locked), клик открывает курс, заблокированные не кликаются (строки 17-39, 435-451).
- ✅ Fallback-список «Списком» (строки 279-336).
- 🟡 Достижим только из VillageView через `is_village`-секции (`LearningPage.tsx:128`). На новой Tactical-карте узел = курс напрямую, посёлки не используются → **VillageView и весь section/village-флоу — параллельная legacy-карта**, доступная лишь по `/learning/legacy`. Дубль навигации (см. п.10).

### Дизайн/UX
- 🔴 **Захардкоженные строки/цвета:** «Назад к посёлку» (строка 285, только RU), inline `#b0bec5/#43a047/#1e88e5` цвета домиков (строки 75-96), `from-slate-800` фон. Не тема-aware, не bilingual в fallback-списке. **🟡P1** (если флоу остаётся живым).
- 🟡 Декор (звёзды, трава, деревья) — мило, но это immersive-сцена внутри светлой платформы; легенда снизу на `text-gray-500` (строки 476-491) при светлой теме нужно проверить контраст.

---

## 8. QuizPage (отдельная страница теста)

**Статус: 🔴 (мёртвый дубль)**
`src/pages/QuizPage.tsx`, маршрут `/quiz/:contentItemId` (`App.tsx:535`)

### Функционал
- 🔴 **Орфанный маршрут.** Поиск по всему `src/` не нашёл НИ ОДНОЙ навигации на `/quiz/:id` или передачи `state.quiz`. Страница берёт данные из `location.state.quiz` (строки 45-46) — без него показывает «Тест не найден». **Недостижима из UI → мёртвый код / дубль квиз-флоу** (квизы реально проходятся внутри CourseView/BlockQuiz/LessonQuizView). **🟡P1** — удалить маршрут+страницу или восстановить вход.
- 🟡 Если бы была достижима: своя реализиция таймера, навигации, результатов — третья независимая реализация квиза (после QuizRenderer и LessonQuizView).

### Дизайн/UX
- 🟡 Эмодзи-тяжёлый (❓📝📋📊⏱✅🚀🎉📚🔄), хедер с собственной кнопкой logout (строки 102-105, 151) — дублирует глобальный Layout. Несогласованно с остальной зоной.

---

## 9. HeroPanel (левая панель десктоп-карты)

**Статус: 🔴**
`src/components/tactical/HeroPanel.tsx`

### Функционал
- 🔴 **Захардкоженные фейковые метрики:** эшелон «Практик», след. рубеж «Эксперт» (строки 81-87), серия «5 дн», «время сегодня 42 мин», «рейтинг #7/142» (строки 98-111). Реальны только `total/done/overallPct` из props. **🔴P0** — те же выдуманные данные, что в мобильном Hero. Статистика курсов (Пройдено/В процессе/Доступно/Заблокировано) тоже частично синтезирована из `done/total` эвристикой (строки 45-48), а не из реальных статусов.
- 🟡 Дефолты `total ?? 32, done ?? 12` (строки 44-45) — magic numbers fallback.

### Дизайн/UX
- ✅ Bilingual (RU/UZ) везде через `t()`. Использует токены.
- 🟡 Zone-navigator (фокус по территории) — рабочий и полезный.

---

## 10. Архитектурный дубль: две карты обучения

**Статус: 🟡P1**

В зоне сосуществуют **две полные реализации навигации по обучению**:
1. **Tactical** (`/learning`): TacticalLearningPage + TacticalMap/TacticalMobile, узел = курс. Основная.
2. **Legacy** (`/learning/legacy`, `/learning/course/:id`): LearningPage modules → map (LearningMap) → section/village (VillageView/SectionView) → course. Глубокая иерархия.

CourseView (плеер) общий и нужен обоим. Но ModulesView/LearningMap/VillageView/SectionView — целая ветка, доступная лишь по прямому URL `/learning/legacy`. Это ~1000+ строк параллельного UI. **Решение PO:** оставить legacy как fallback или удалить. Дубли = двойная поддержка + расхождение поведения.

---

## 11. Узлы-инструктажи «Гид» (онбординг)

**Статус: ✅**
`src/components/onboarding/GuidesPanel.tsx`

- ✅ Корректно загатчен: рендерится только при `PROGRESSIVE_DISCLOSURE_ENABLED` И для `GATING_ROLES` (строки 33-34). Сейчас флаг выключен (по памяти `progressiveDisclosure.ts` на ~60%, выключен) → панель не видна в проде. Это ожидаемо.
- ✅ Пройденные гиды в localStorage по userId (строки 26-45). Чисто.
- 🟡 Эмодзи 🎁 + `g.icon` как иконки. Tour-карточки (GuideTour) не аудировались отдельно — вне основного потока, флаг off.

---

## Backend

**Статус: ✅**
`app/api/v1/learning.py` (1882 стр), `app/services/lesson_pipeline.py` (2574 стр)

- ✅ `POST /courses/{id}/complete` (строки 871-1099): полный, корректный — дубль-чек (409), пересчёт avg/streak/level/section/competency, field_task closed-loop, Celery KPI+pulse ПОСЛЕ commit (важный порядок, есть коммент). Все вспомогательные вызовы в try/except с `logger.warning` — не ломают завершение урока. Грамотно.
- ✅ `POST /auto-assign` (строки 1560-1734): реальная логика weak-zones (KPI/тесты/ассессменты/competency-gap) + RAG-рекомендации. Не заглушка.
- ✅ Нет TODO/FIXME/NotImplemented/mock в learning.py. `use_mock` в lesson_pipeline.py (строки 794-799) — легитимный тест-флаг.
- ✅ Озвучка в отдельном `api/v1/narration.py` + `tasks/narration_tasks.py` + `services/tts_service.py` (ElevenLabs). Не аудировалась глубоко, но фронт корректно silent-fail при `status != 'ready'`.

---

## ТОП-5 находок

1. 🔴 **P0 — Фейковые геймификационные метрики у пользователя.** HeroPanel (десктоп, `HeroPanel.tsx:81-111`) и HeroStrip/FriendsStrip (мобайл, `TacticalMobile.tsx:963-970, 583-635`) показывают захардкоженные XP/серию/лигу/рейтинг/«активность команды» (ЗОЛОТО II, #7/142, Аиша взяла Эксперта…). Без пометки «демо». Подрывает доверие к платформе. → Подключить реальные данные (KPI/pulse/leaderboard уже есть в проде) или скрыть до интеграции.

2. 🔴/🟡 **P1 — Захардкоженные строки и tailwind-цвета вместо токенов и bilingual.** LessonDataView (`LessonDataView.tsx`: «Ситуация»/«Как правильно?»/«correct» — только RU/EN), VillageView, CourseView results/slides используют `text-gray-*/bg-blue-*` вместо токенов темы → ломают светлую «Пепел-холст» и UZ. → Прогнать обвязку (не immersive-сцены) через токены + `bl()`/`t()`.

3. 🟡 **P1 — Мёртвый QuizPage и орфанный маршрут `/quiz/:contentItemId`.** Недостижим из UI (нет навигации/передачи state). Третья независимая реализация квиза. → Удалить страницу+маршрут или восстановить вход.

4. 🟡 **P1 — Две параллельные карты обучения.** Tactical (`/learning`, основная) vs Legacy (`/learning/legacy`: modules→map→village→section, ~1000+ строк). Доступна лишь по прямому URL. → Решение PO: оставить как fallback или удалить + мёртвый код рёбер в TacticalMap.

5. 🟡 **P1 — Монотонные статусы пинов на карте.** Цвет пина = тир, а не прогресс; backend почти всегда отдаёт `available` → пользователь не видит «где он сейчас» (`TacticalMap.tsx:16-24`, `mapLearningToNodes.ts`). Известно из памяти. → Включить gating/прогрессию состояний курсов на бэке, затем отразить дизайном (глиф/пульс уже готовы).
