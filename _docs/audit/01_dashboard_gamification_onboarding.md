# Аудит зоны 01 — Дашборд + Геймификация + Онбординг

> Read-only аудит. Код НЕ редактировался. Дата: 2026-06-28.
> Фронт: `D:\Траектория\traektoriya-new` · Бэк: `D:\Траектория\traektoriya-rag-backend`
> Зоны проверки: DashboardPage, виджеты дашборда, Power (Мощь), онбординг (сундуки/гид/прогрессивное раскрытие), бэкенд power_service.

Легенда приоритетов: 🔴P0 (сломано/блокер) · 🟡P1 (важно) · 🟢P2 (полировка).

---

## Сводка по фичам

| Фича | Статус | Главное |
|------|--------|---------|
| DashboardPage | ✅ | Минималистичен (Рейтинг + Квесты), чисто |
| LearningRankWidget (лидерборд) | 🟡 | Работает; есть мелкие хардкод-цвета, эмодзи в метриках |
| PowerBadge (Мощь, верхняя полоса) | 🟡 | Работает; хардкод fallback-цвета, поповер по hover проблемен на мобайле |
| PowerWidget (большой) | 🔴 | **Мёртвый код** — нигде не смонтирован |
| ActivityWidget | 🔴 | **Мёртвый код** + ShelfScan-плитки (заморожен) |
| PulseWidget | 🔴 | **Мёртвый код** |
| TasksNotificationsWidget | 🔴 | **Мёртвый код** |
| NudgesWidget | 🔴 | **Мёртвый код** + хардкод светлой темы (`bg-white`) |
| StreakAchievementWidget | 🔴 | **Мёртвый код** (хотя сам по теме чистый) |
| DailyQuestsWidget (квесты дня) | ✅ | Работает, чисто, theme-aware |
| Онбординг: SectionUnlockChest | 🔴 | Смонтирован, но зовёт **несуществующий на проде эндпоинт** claim-tier-bonus |
| Онбординг: GuideTour + GuidesPanel | ✅ | Работают, чисто, на токенах |
| Онбординг: platformGuides.ts | ✅ | Контент-конфиг, чисто |
| Прогрессивное раскрытие (progressiveDisclosure.ts + scopeStore) | 🟡 | Работает; латентный риск редирект-петли на gated dashboard |
| Бэкенд: power_service.py + api/v1/power.py | 🟡 | GET /my OK; **POST /claim-tier-bonus отсутствует в main** |

---

## 1. DashboardPage — ✅

`src/pages/DashboardPage.tsx` (41 строка). По решению PO (v5, 2026-06-25) упрощён до двух блоков:
`LearningRankWidget` + `DailyQuestsWidget`.

**Функционал:** работает. Заголовок/подзаголовок локализованы, дата по локали. Никаких заглушек.

**Дизайн/UX:** соответствует курсу на минимализм — внешнюю двойную рамку убрали (`DashboardPage.tsx:32-33`). Чисто.

**Находка (🟢P2):** «Моя мощь» переехала в `PowerBadge` (верхняя полоса) — это значит большой `PowerWidget` больше не нужен, но файл остался (см. ниже мёртвый код).

---

## 2. LearningRankWidget (лидерборд) — 🟡

`src/components/dashboard/LearningRankWidget.tsx` (552 строки). Живой, используется в DashboardPage.

**Функционал:** ✅ Работает корректно.
- Загрузка лидерборда с обработкой loading/error/empty (`:95-120`).
- Role-гейтинг селектора ролей: только admin/superadmin/commercial_dir (`:34, :188`).
- Демо-аккаунт seller1 фильтруется из выдачи для всех, кроме самого себя (`:124-128`) — корректно.
- Пьедестал ТОП-3 + «мой ранг» + список — логика разбиения чистая.

**Дизайн/UX:**
- 🟡P1 Хардкод-цвета в нескольких местах: `bg-amber-400 text-[#0a1929]` для активных кнопок периода/роли (`:200, :226`), `text-emerald-300/amber-300/blue-300` в формуле (`:168-172` — частично на токенах, частично нет), `text-orange-300` (`:362`). На светлой теме «Пепел-холст» это даёт неконтролируемый контраст — нужно проверить WCAG AA.
- 🟢P2 Бренд-цвета медалей (золото/серебро/бронза `#FBBF24/#E5E7EB/#FB923C`, `:46-50`) — намеренные, комментарии это оговаривают, оставить.
- 🟢P2 Эмодзи в служебных метриках: `📚 {learning} · 🔥 {activity} · ⏱ {streak}` (`:465-469, :538`). По правилу проекта «NO emoji в UI» (frontend.md) — это нарушение, лучше иконки. Но это компактные индикаторы формулы, не критично.
- 🟢P2 Захардкоженные русские подписи мимо i18n: «Преследователи» (`:378`), «Твой ранг» (`:340`), «(вы)» (`:517`), «из {total}» — при UZ-локали останутся на русском. Лидерборд частично двуязычен (через `t()`), частично нет — непоследовательно.

---

## 3. PowerBadge (Мощь в верхней полосе) — 🟡

`src/components/tactical/PowerBadge.tsx` (195 строк). Смонтирован в `StatusBar.tsx:170` (десктоп). Живой.

**Функционал:** ✅
- Модульный кэш с TTL 60с + дедупликация in-flight запросов (`:24-43`) — хорошо.
- `invalidatePowerBadge()` экспортирован для сброса после действий (`:46-48`), но **нигде не вызывается** (🟢P2 — потенциально Мощь в бейдже не обновляется после квеста/урока до истечения TTL/перезагрузки).
- Тихий fail при ошибке (`:61` — «бейдж просто не показываем»). Приемлемо для некритичного элемента.

**Дизайн/UX:**
- 🟡P1 Поповер открывается по `onMouseEnter/onMouseLeave` (`:100-101`) — на мобайле hover не работает; есть fallback на `onClick` (`:105`), но комбинация hover+click даёт «дёрганье» на тач-устройствах. Стоит развести десктоп/мобайл.
- 🟡P1 Хардкод fallback-цвета в токенах: `var(--color-rm, #c08a4a)`, `var(--text-1, #b8c0cf)`, `var(--warning, #f2c660)` (`:16-20, :90-93`) — fallback-значения тёмной темы; если токен не определён в светлой теме, цвет уедет. Проверить, что все эти токены заданы в обеих темах.
- 🟢P2 Шрифт `'JetBrains Mono', monospace` хардкодом (`:118, :154`) вместо токена `--font-mono`.
- 🟢P2 Поповер использует `var(--bg-1, #11161f)` — тёмный фон-fallback в светлой теме = чёрный поповер, если токен не переопределён.

---

## 4. PowerWidget (большой виджет Мощи) — 🔴 МЁРТВЫЙ КОД

`src/components/dashboard/PowerWidget.tsx` (164 строки).

**Находка (🔴P0 как мусор / 🟢P2 как риск):** компонент **не импортируется и не монтируется нигде** (проверено grep по всему `src/`). После переезда Мощи в `PowerBadge` (PO 2026-06-25) большой виджет осиротел. Это не ломает прод (не рендерится), но:
- мёртвый код вводит в заблуждение (дублирует логику PowerBadge);
- содержит ту же `nextUnlocks`-интеграцию (`:5, :123`), которая больше нигде не нужна.

**Рекомендация:** удалить файл (или явно задокументировать как «архив»). Логику Мощи держать в одном месте — PowerBadge.

---

## 5. ActivityWidget — 🔴 МЁРТВЫЙ КОД

`src/components/dashboard/ActivityWidget.tsx` (303 строки). **Не монтируется нигде.**

**Дополнительные находки (на случай возврата в строй):**
- 🟡P1 Плитка ShelfScan (`:191-199`, ссылка `/planogram`) — ShelfScan заморожен и Планограмма admin-only (scopeStore `ROLE_FORCE_DENY.planogram`). Для ТП ссылка ведёт в запрещённый раздел.
- 🟡P1 Хардкод-цвета классами: `text-emerald-300/amber-300/blue-300/text-white/30` (`:144-149`), `bg-amber-400` (`:175`) — поедут на светлой теме.
- 🟢P2 «CRM-заглушка» (`:232-251`) — честный placeholder «Скоро», ок, но это ещё одна копия формулы 50/30/20 (дублируется с LearningRank и Pulse — три источника правды одной формулы).

**Рекомендация:** удалить либо, если планируется вернуть, вынести в отдельную страницу активности (не на минималистичный дашборд).

---

## 6. PulseWidget — 🔴 МЁРТВЫЙ КОД

`src/components/dashboard/PulseWidget.tsx` (439 строк). **Не монтируется нигде.**

**Дополнительные находки:**
- ✅ Сам по себе хорошо написан: retry с backoff (`:113-118`), AbortController, aggregate-режим для admin, обработка empty/loading.
- 🟡P1 Много хардкод-цветов `#4ADE80/#60A5FA/#FBBF24/#EF4444` в `LEVEL_META` (`:30-35`) и `hover:bg-white/[0.02]` (`:289, :384`) — light-theme риск.
- 🟢P2 Эмодзи в KPI-плитках (`📚🏆⚡🎯`, `:316-339`).

**Рекомендация:** функциональность дублируется со страницей `/competencies`. Удалить виджет либо подтвердить, что Pulse намеренно убран с дашборда (комментарий в DashboardPage это подтверждает: «Убраны… Пульс компетенций»).

---

## 7. TasksNotificationsWidget — 🔴 МЁРТВЫЙ КОД

`src/components/dashboard/TasksNotificationsWidget.tsx` (380 строк). **Не монтируется нигде.**

- ✅ Аккуратно сделан: SVG-иконки вместо эмодзи (правильно по гайдлайну), theme-aware токены (комментарий `:12`), сортировка по срочности.
- 🟡P1 Остаточные хардкод-цвета статусов `#EF4444/#FB923C/#FBBF24/#60A5FA` (`:24-31`) — для статусных полос приемлемо (семантика), но `color: '#FBBF24'` на ссылках «Открыть Канбан» (`:229, :274`) на светлой теме — низкий контраст.
- Захардкоженные русские строки мимо i18n («Просрочено», «Все задачи выполнены!» и т.д.) — при UZ останутся русскими.

**Рекомендация:** удалить (задачи теперь живут на `/tasks`).

---

## 8. NudgesWidget — 🔴 МЁРТВЫЙ КОД + светлая тема сломана

`src/components/dashboard/NudgesWidget.tsx` (88 строк). **Не монтируется нигде.**

**Находки:**
- 🔴 (как баг светлой темы, если вернуть) Полностью на хардкод-классах старой палитры: `bg-white` (`:29`), `text-gray-800/900/500/400` (`:37, :62-63`), `bg-blue-100 text-blue-600` (`:32`), `bg-gray-50` (`:54`), `text-blue-600` (`:41, :67`). **Это единственный виджет зоны, не переведённый на CSS-токены.** На тёмной теме будет белая карточка с серым текстом — нечитаемо. Резко выбивается из остальных (theme-aware) виджетов.
- Дублирует функцию уведомлений (nudges) — на дашборде их решили не показывать.

**Рекомендация:** удалить. Если возвращать — полная переверстка на токены обязательна.

---

## 9. StreakAchievementWidget — 🔴 МЁРТВЫЙ КОД (но сам по теме чистый)

`src/components/dashboard/StreakAchievementWidget.tsx` (173 строки). **Не монтируется нигде.**

- ✅ Хорошо: на токенах (`TIER_STYLES` через `var(--…)`, `:8-13`), SVG-иконка заголовка, обработка loading/empty.
- 🟢P2 Эмодзи в плитках (🔥🏆⭐) через HTML-entity (`:96, :106, :116`) — допустимо.
- Дублирует streak/achievements, которые уже отражены в PowerBadge (breakdown) и на `/goals`.

**Рекомендация:** удалить либо сознательно сохранить для будущей страницы достижений.

---

## 10. DailyQuestsWidget (квесты дня) — ✅

`src/components/dashboard/DailyQuestsWidget.tsx` (99 строк). Живой, используется в DashboardPage.

**Функционал:** ✅
- Фильтрует квесты по `type==='daily_quest'` и сегодняшней ташкентской дате (UTC+5, `:19-24, :36-38`) — корректная таймзона.
- Скрывает панель целиком, если квестов нет (`:46`) — минимализм соблюдён.
- Навигация по категории квеста (`questTarget`, `:12-17`).

**Находки:**
- 🟢P2 `questTarget` для `category==='products'` ведёт на `/products`, для `tasks` → `/tasks`. Но эти разделы могут быть tier-gated/скрыты для бронзового ТП (progressiveDisclosure) → клик по квесту приведёт на редирект. Стоит сверить, что квест не ведёт в закрытый раздел.
- 🟢P2 Авто-отметка «выполнено» — на бэке (комментарий `:9-10`). Здесь только отображение — ок, но нет визуального фидбэка о начислении XP при клике.
- ✅ Theme-aware (токены `var(--success-bg)` и т.д.).

---

## 11. Онбординг: SectionUnlockChest (сундук-разблокировка) — 🔴

`src/components/onboarding/SectionUnlockChest.tsx` (250 строк). Смонтирован глобально в `App.tsx:598`. Живой.

**🔴P0 — ГЛАВНАЯ НАХОДКА ЗОНЫ:** при открытии сундука вызывается
`powerApi.claimTierBonus()` → `POST /api/v1/power/claim-tier-bonus` (`SectionUnlockChest.tsx:48`, `api/power.ts:40-41`).
**Этого эндпоинта НЕТ в основном бэкенде** `app/api/v1/power.py` (там только `GET /my`).
Он существует ТОЛЬКО в worktree `traektoriya-rag-backend/_wt/pb2/app/api/v1/power.py:86-155` (не задеплоен).

Последствия:
- В проде вызов вернёт **404**.
- Ошибка проглатывается `.catch()` (`:48-51`) → сундук всё равно открывается, но **бонус «+N Мощи» никогда не показывается** (`bonus` остаётся 0, блок `:177` не рендерится).
- То есть обещанная механика «сундук даёт +250/+500/+1000 Мощи» **не работает на проде** — тихий silent-fail.

**Рекомендация (🔴P0):** либо задеплоить `claim-tier-bonus` из `_wt/pb2` в main backend, либо убрать вызов из фронта, чтобы не давать ложное обещание. Сейчас фронт и бэк рассинхронизированы.

**Прочее:**
- ✅ Логика «празднуем рост тира один раз» через localStorage по userId (`:62-85`) — корректна, идемпотентна.
- ✅ Анимации на токенах, двуязычно.
- 🟢P2 `data-screenshot-ignore` — для системы фидбека, ок.

---

## 12. Онбординг: GuideTour + GuidesPanel + platformGuides.ts — ✅

- `GuidesPanel.tsx` (94 стр.) смонтирован в `TacticalLearningPage.tsx:241` и `TacticalMobile.tsx:986`. Живой.
- `GuideTour.tsx` (128 стр.) — оверлей-тур карточками.
- `platformGuides.ts` (215 стр.) — 6 гидов, двуязычно.

**Функционал:** ✅
- Гейтинг по флагу + полевым ролям (`GuidesPanel.tsx:33-34`).
- «Пройдено» через localStorage по userId (`:26-31, :39-45`).
- Накопительная выдача гидов по тиру (`guidesUpToTier`, platformGuides `:211-215`).

**Дизайн/UX:** ✅ Полностью на токенах, чисто, минималистично. Эмодзи здесь уместны (это онбординг-иллюстрации, не UI-управление).

**Находка (🟢P2):** `GuidesPanel` зависит от `userTier` из scopeStore, который заполняется только при `PROGRESSIVE_DISCLOSURE_ENABLED`. Если флаг выключен для эксперт-ревью — гиды не покажутся вообще (по дизайну, но стоит помнить).

---

## 13. Прогрессивное раскрытие (progressiveDisclosure.ts + scopeStore.ts) — 🟡

`src/config/progressiveDisclosure.ts` (205 стр.) + `src/stores/scopeStore.ts` (161 стр.).

**Функционал:** в целом ✅, продумано (UX-слой поверх role-scopes, не даёт доступа сверх роли).

**Находки:**
- 🟡P1 **Латентный риск редирект-петли.** `dashboard` гейтится при `silver` для полевых ролей (`progressiveDisclosure.ts:73`). Бронзовый ТП на `/dashboard` → `FullscreenProtectedRoute` (`App.tsx:194`) видит `!isPageAllowed('dashboard')` → редирект на `getFirstAllowedPath()`. Эта функция итерирует `allowedPages` и возвращает первую прошедшую проверку (обычно `learning` — не гейтится), что спасает. **НО** финальный fallback — `return '/dashboard'` (`scopeStore.ts:157`). Если по какой-то причине `learning` не окажется в `allowedPages` (или список пуст после фильтрации), бронзовый ТП получит редирект на gated dashboard → снова редирект → **петля / чёрный мигающий экран** (этот же класс бага уже описан в комментарии `:148-150`). Сейчас спасает только то, что learning всегда разрешён. Стоит сделать fallback на гарантированно-негейченный раздел (`/learning`).
- 🟢P2 Флаг включён по умолчанию (`:33`, «ВКЛ с 2026-06-28»). Значит онбординг-гейтинг активен на проде — все находки выше про сундук (404) сейчас реально срабатывают у полевых ролей.
- ✅ `fetchUserTier` корректно дёргается из authStore после логина/восстановления сессии (`authStore.ts:76, :117`).
- ✅ `isPageAllowed` композирует base role-scope + ROLE_FORCE_DENY + tier-gating — логично.

---

## 14. Бэкенд: power_service.py + api/v1/power.py — 🟡

`app/services/power_service.py` (136 стр.) + `app/api/v1/power.py` (68 стр.).

**Функционал:** ✅ для того, что есть.
- `GET /power/my` — read-only расчёт Мощи на лету из KPI/обучения/достижений/серий, без новых таблиц (`power_service.py:63-135`). Кэш 300с (`power.py:59-67`). Router подключён (`main.py:408`). Корректно.
- Бизнес-доминантные веса, накопительный характер — соответствует решению PO.

**Находки:**
- 🔴P0 (связка с фронтом) **`POST /claim-tier-bonus` отсутствует в main** (`app/api/v1/power.py` обрывается на `GET /my`, строка 68). Реализация лежит в `_wt/pb2/app/api/v1/power.py:86-155` и **не задеплоена**. Это первопричина silent-fail сундука (см. фичу 11). Логика бонуса там корректна (идемпотентность через UniqueConstraint achievement_code, self-seed Achievement), её просто нужно перенести/задеплоить.
- 🟢P2 `kpi_total or 0.0` (`power_service.py:72-77`) — если записи KPI нет, business=0. Для нового ТП Power будет почти полностью из learning/streak → тир bronze. Это ожидаемо, но значит «business-доминанта» для новичка не работает (нечем). Норма.
- 🟢P2 Приватные имена `_current_period`, `compute_power` импортируются в роутер (`power.py:21`) — мелочь, ок.

---

## ТОП-5 находок зоны

1. 🔴 **P0 — Сундук-разблокировка обещает бонус Мощи, которого нет на проде.** Фронт (`SectionUnlockChest.tsx:48`) зовёт `POST /api/v1/power/claim-tier-bonus`, но эндпоинт есть только в незадеплоенном worktree `_wt/pb2` — в main backend (`app/api/v1/power.py`) его НЕТ. На проде → 404, проглатывается `.catch()`, «+N Мощи» никогда не показывается. Механика геймификации заявлена, но не работает. **Фикс:** задеплоить эндпоинт из `_wt/pb2` или убрать вызов.

2. 🔴 **P0 (чистота кода) — 6 виджетов дашборда = мёртвый код.** `PowerWidget`, `ActivityWidget`, `PulseWidget`, `TasksNotificationsWidget`, `NudgesWidget`, `StreakAchievementWidget` (~1670 строк суммарно) не импортируются нигде — DashboardPage использует только `LearningRankWidget` + `DailyQuestsWidget`. Раздувают бандл и сбивают с толку. **Фикс:** удалить (или явно заархивировать).

3. 🟡 **P1 — NudgesWidget полностью ломает темизацию.** Единственный виджет зоны на старых хардкод-классах (`bg-white`, `text-gray-800`, `bg-blue-100`) вместо CSS-токенов (`NudgesWidget.tsx:29-67`). На тёмной теме — белая нечитаемая карточка. Хотя сейчас он мёртвый, при возврате сломает обе темы. Остальные виджеты уже на токенах — он выбивается.

4. 🟡 **P1 — Латентная редирект-петля на gated dashboard.** `getFirstAllowedPath()` имеет финальный fallback `/dashboard` (`scopeStore.ts:157`), а dashboard tier-gated при silver для полевых ролей. Бронзового ТП спасает только то, что `learning` всегда в allowedPages; при пустом/отфильтрованном списке — петля редиректов (чёрный мигающий экран). **Фикс:** fallback на `/learning`.

5. 🟡 **P1 — Хардкод-цвета и эмодзи vs светлая тема «Пепел-холст».** В живых LearningRankWidget и PowerBadge есть классы `bg-amber-400 text-[#0a1929]`, `text-emerald-300/amber-300`, `text-orange-300` и токен-fallback'и тёмной палитры (`var(--bg-1, #11161f)`), плюс эмодзи в служебных метриках формулы. На светлой теме контраст не гарантирован (нужна проверка WCAG AA ≥4.5), эмодзи нарушают правило «NO emoji в UI». **Фикс:** перевести на семантические токены, метрики — на иконки.
